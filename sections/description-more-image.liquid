{{ 'component-description-more-image.css' | asset_url | stylesheet_tag }}

<div class="description-more-image">
  <div class="description-wrapper">
    <div class="tabs-container">
      <ul class="tabs">
        <li class="tab active" data-tab="details">DETAILS</li>
        <li class="tab" data-tab="care-guide">CARE GUIDE</li>
        <li class="tab" data-tab="shipping">SHIPPING</li>
      </ul>

      <div id="details" class="tab-content active">
        <p>{{ block.settings.tab_content }}</p> 
      </div>

      <div id="care-guide" class="tab-content">
        <p>{{ block.settings.care_guide_text }}</p>
      </div>

      <div id="shipping" class="tab-content">
        <p>{{ block.settings.shipping_text }}</p>
      </div>
    </div>

    {% if section.settings.show_image %}
      <div class="image-right">
        <img src="{{ section.settings.image | image_url }}" alt="{{ section.settings.image_alt }}" />
      </div>
    {% endif %}
  </div>
</div>

<script src="{{ 'description-more-image.js' | asset_url }}" defer></script>

{% schema %}
{
  "name": "Description More Image",
  "settings": [
    {
      "type": "checkbox",
      "id": "show_image",
      "label": "Show Image",
      "default": true
    },
    {
      "type": "image_picker",
      "id": "image",
      "label": "Upload Image"
    },
    {
      "type": "text",
      "id": "image_alt",
      "label": "Image Alt Text",
      "default": "Default alt text for image"
    }
  ],
  "blocks": [
    {
      "type": "tab",
      "name": "Tab",
      "settings": [
        {
          "type": "text",
          "id": "tab_title",
          "label": "Tab Title",
          "default": "Description"
        },
        {
          "type": "richtext",
          "id": "tab_content",
          "label": "Tab Content",
          "default": "Essentials for Long-Lasting Shoes..."
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Description More Image",
      "category": "Custom"
    }
  ]
}

{% endschema %}
